FROM python:3.10-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx pyyaml

COPY splitter.py config_ab.yaml ./

CMD ["uvicorn", "splitter:app", "--host", "0.0.0.0", "--port", "8001"]
